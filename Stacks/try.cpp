#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    string reverseWords(string s) {
        stack<char> s
            
        s.push("Try");
        s.push("to");
        s.push("excel.");
        
        cout<<s.top();
        s.pop()
            
        cout<<s.top();
        s.pop()
            
        cout<<s.top();
        s.pop()
            
        return 0;
    }
};